services:
  nginx:
    build:
      dockerfile: ./nginx
    ports:
      - '443:443'
      - '80:80'
  mongo:
    image: mongo:4.2.5
  myapp:
    build: .
    environment:
      WAIT_HOSTS: mongo:27017
      DOCKER_DB: true
    ports:
      - '8080:8080'